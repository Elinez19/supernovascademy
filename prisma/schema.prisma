// Prisma schema for Supernovas Academy
// Better Auth CLI will extend this with the required models.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


